// Type definitions for LayoutElement
export const LayoutElement = {
  // This is a placeholder - the actual interface is used by TypeScript
  // but we need this export to satisfy the import statements
};
